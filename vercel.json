{
  "buildCommand": "sed -i 's/\\r$//' scripts/vercel-build.sh 2>/dev/null || true; bash scripts/vercel-build.sh",
  "outputDirectory": "build/web",
  "installCommand": "if [ -d flutter ]; then cd flutter && git pull && cd ..; else git clone https://github.com/flutter/flutter.git; fi && flutter/bin/flutter doctor && flutter/bin/flutter config --enable-web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
